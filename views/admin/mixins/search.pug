mixin search(keyword)
 form(id="form-search")
          .input-group
            input(
              type="text"
              placeholder="Nhập từ khóa"
              name="keyword"
              class="form-control"
              value = keyword
            )
            //- cái phần value = keyword đấy để khi người dung nhập từ ô tìm kiếm thì giá trị vừa nhập không bị mất đi 

            .input-group-append
              button(
                class="btn btn-success"
                type="submit"
              ) Tìm