mixin select-tree(items, level = 1, parent_id = "")
  each item in items 
    - const prefix = Array(level + 1).join("-- ")
    option(
      value = item.id 
      selected = (item.id == parent_id ? true : false)
      ) #{prefix} #{item.title}
      if (item.children && item.children.length > 0)
        //-  do cấp đệ quy đầu tiên chưa có parent_id nên ta sẽ chuyền parent_id ở lần đệ quy thứ 2
        +select-tree(item.children, level + 1, parent_id) 