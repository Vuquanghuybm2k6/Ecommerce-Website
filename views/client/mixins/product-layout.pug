include product-item.pug
mixin product-grid(products)
  .row
    each item in products
      +product-item(item)
       //- tách nhỏ và include thêm mixin khác để nếu mai này dự án phát triển hơn thì ta sẽ có nhiều layout khác